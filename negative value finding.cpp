#include<stdio.h>
int main()
{
	int i,j,n,arr[n];
	printf("enter the size");
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&arr[i]);
	}
		for(i=0;i<n;i++)
	{
		printf(" array is %d",arr[i]);
	}
	//finding negative value in given array
printf("negative value in given array");
	for (i=0;i<n;i++)
	{
		if(a[i] < 0)
		{ 
			printf("%d\t",a[i]);
		}
	}
	
}
